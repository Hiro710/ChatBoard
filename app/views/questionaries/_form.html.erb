<%= form_for(questionary) do |f| %>
	<% if questionary.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(questionary.errors.count, "error") %> prohibited this questionary from being saved:</h2>

			<ul>
			<% questionary.errors.full_messages.each do |message| %>
				<li><%= message %></li>
			<% end %>
			</ul>
		</div>
	<% end %>

	<div class="field">
		<%= f.label :title %>
		<%= f.text_field :title, {size:40} %>
	</div>

	<div class="field">
		<%= f.label :description %>
		<%= f.text_area :description, {cols:40, rows:5} %>
	</div>

	<div class="field">
		<%= f.label :deadline %>
		<%= f.datetime_select :deadline %>
	</div>

	<div class="actions">
		<%= f.submit %>
	</div>
<% end %>
